numeros <- c(1:3)

#Atribuindo novo valor ao número
numeros <- c(numeros,3.3)

#Vetor só pode ter um tipo de dado 
#Vetor tava todo integer, colocou o 3.3, vira numeric

numeros <- c(numeros, 4L) 

#Adicionando String no vetor
numeros <- c(numeros, "18")
#Vai converter TUDO pra caracter



#Atribuir 
