#Vetor a ser calculado
x <- c(1:10)

#Soma
x + 10 #Soma 10 em todos os valores do vetor 
xMultiplicado <- x*10


#Multiplicação de vetores
mult <- c(2:11)
vetoresmultiplicados <- mult*xMultiplicado #multiplica os valores de mesma posição

#vetor menor que o alvo
y <- c(1:3)
#Ajustar y
y <- c(2,3)

x*y #(vai multiplicar y1, y2, y1,y2, e assim, sucessivamente)
